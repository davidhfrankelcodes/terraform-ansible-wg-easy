---
- hosts: all
  become: true
  tasks:
    # 1️⃣ Remove old docker.io (if present)
    - name: Remove old docker.io
      apt:
        name: docker.io
        state: absent
        purge: yes
      ignore_errors: yes

    # 2️⃣ Install required packages
    - name: Install required packages
      apt:
        name:
          - ca-certificates
          - curl
          - gnupg
          - lsb-release
        state: present
        update_cache: yes

    # 3️⃣ Add Docker GPG key
    - name: Add Docker GPG key
      shell: |
        mkdir -p /etc/apt/keyrings && \
        curl -fsSL https://download.docker.com/linux/debian/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
      args:
        creates: /etc/apt/keyrings/docker.gpg

    # 4️⃣ Add Docker APT repository (corrected to use ansible_lsb.codename!)
    - name: Add Docker APT repository
      ansible.builtin.lineinfile:
        path: /etc/apt/sources.list.d/docker.list
        line: "deb [arch=amd64 signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian {{ ansible_lsb.codename }} stable"
        create: yes

    # 5️⃣ Install docker-ce and docker compose plugin
    - name: Install docker-ce and docker compose plugin
      apt:
        name:
          - docker-ce
          - docker-ce-cli
          - containerd.io
          - docker-buildx-plugin
          - docker-compose-plugin
        state: present
        update_cache: yes

    # 6️⃣ Add 'admin' user to docker group
    - name: Add 'admin' user to docker group
      user:
        name: admin
        groups: docker
        append: yes

    # 7️⃣ Ensure docker is enabled and started
    - name: Ensure docker is enabled and started
      systemd:
        name: docker
        enabled: true
        state: started

    # ✅ Copy docker directory
    - name: Copy docker directory contents to /home/admin/docker
      copy:
        src: "../docker/"
        dest: "/home/admin/docker/"
        owner: admin
        group: admin
        mode: "0755"
        directory_mode: "0755"

    # ✅ Copy .env file
    - name: Copy .env file
      copy:
        src: "../docker/.env"
        dest: "/home/admin/docker/.env"
        owner: admin
        group: admin
        mode: "0600"

    # ✅ Run docker compose
    - name: Run docker compose
      shell: |
        cd /home/admin/docker && docker compose up -d
      args:
        executable: /bin/bash
